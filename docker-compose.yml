services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      # 无法联网获取行情时可改为 "1" 使用演示数据
      USE_MOCK_DATA: "0"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
